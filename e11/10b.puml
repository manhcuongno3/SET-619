@startuml 10a
start
:Nhập mảng n;
:set min = n[0];
:set index = 0;
:set i = 1;
while (i < n.length) is (Yes)
    if (n[i] < min) then (Yes)
        :set min = n[i];
        :set index = i;
    endif
    :set i = i + 1;
endwhile (No)
:In ra index;
stop
@enduml
